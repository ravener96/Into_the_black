{
  "name": "into-the-black",
  "version": "2.0.0",
  "description": "CSS compiler for the itb system",
  "scripts": {
    "build": "sass src/scss/into-the-black.scss css/into-the-black.css --style=expanded --no-source-map",
    "watch": "sass src/scss/into-the-black.scss css/into-the-black.css --style=expanded --source-map --watch"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "Asacolips",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "sass": "^1.53.0"
  }
}